<!-- this actually goes through and labels the comments -->
<%= form_for([@message, @message.comments.build]) do |f| %>
 <%= f.hidden_field :commenter_id, :value => current_user.id %>

  <p>
    <%= f.label :Comment %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>